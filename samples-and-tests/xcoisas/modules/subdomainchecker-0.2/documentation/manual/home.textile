h1. {module name}

Subdomain checks the subdomain name for each request.

h2. Usage

Include in application.conf and just works!
%env.default.subdomain=<subdomain>

If base url aready have a subdomain this settings will be ignored!

